# The built-in OpenAI-compatible adapter reads this key. Use a server-only env file.
OPENAI_API_KEY=

# Optional: trace chats and manage the system prompt in Langfuse.
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=https://cloud.langfuse.com
